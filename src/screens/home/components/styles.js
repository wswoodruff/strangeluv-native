const { StyleSheet } = require('react-native');

module.exports = StyleSheet.create({

    title: {
        fontSize: 19,
        fontWeight: 'bold',
        textAlign: 'center'
    },

    menuBtn: {
        padding: 10
    },

    duck: {
        marginTop: 20,
        width: 120,
        height: 120,
        resizeMode: 'contain'
    }
});
